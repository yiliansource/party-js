import LiveCodeblock from "../components/LiveCodeblock";

# New

<LiveCodeblock>

```js
party.confetti(mouseEvent, {
    count: 50,
});
```

</LiveCodeblock>

# Old

```js
party.confetti(mouseEvent, {
    count: 50,
});
```
