import { ITickable } from "./interfaces";
import particleFactory from "./particleFactory";
import scene from "./scene";

export default class Emitter implements ITickable {
    public tick(delta: number) {
    }
}